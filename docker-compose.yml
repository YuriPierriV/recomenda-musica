services:
  music-recomendation-service:
    build: ./music-recomendation-service
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ./music-recomendation-service/:/app/
    ports:
      - 8000:8000